# HANDOVER — 철면수심 먹방 룰렛

> 다음 세션(사람/AI 누구든)이 이 문서만 읽고 이어서 작업할 수 있도록 남기는 인수인계 문서.
> 마지막 업데이트: **2026-09-18** · 상태: **개발 완료 · 철면수심 계정 실도네 수신까지 검증 완료**

## 1. 한눈에 보기

| 항목 | 값 |
|---|---|
| 라이브 | https://goormigrm.github.io/mukbang-roulette/ |
| 저장소 | https://github.com/goormigrm/mukbang-roulette (main push → Actions 자동 배포) |
| 프록시 워커 | https://mukbang-proxy.1117tkdrms.workers.dev (Cloudflare, 계정 1117tkdrms) |
| 치지직 앱 | 개발자센터 "먹방룰렛" · Client ID `95337781-0490-4c9b-ac9a-9577a9ef4db0` · 스코프: 유저 조회, 후원 조회 |
| 스택 | Vite + Vanilla TS + Canvas 2D, socket.io-client **v2.5.0 고정**(치지직 세션이 Socket.IO 2.0.3까지만 지원) |
| 상세 설계 | [DESIGN.md](DESIGN.md) — 모든 규칙 결정 사항과 변경 이력 포함 |

**연동 검증 상태**: ✅ **실전 검증 완료 (2026-09-18)** — 철면수심 님 계정으로 로그인해 **실제 도네이션이 룰렛에 반영되는 것까지 정상 동작 확인**. **방송 운영 방식: 크롬 창에서 그대로 띄워 쓴다**(별도 캡처 구성 없음 — 이 방식으로 실방송 검증을 마쳤으므로 다시 제안하지 말 것).

## 2. 확정된 운영 규칙 (전부 구현·테스트 완료)

- 도네 1,000원 = 1칸, 메시지가 메뉴명(비면 "닉네임의 추천"), 같은 이름은 칸 합산. 최소 1,000원, 상한 없음, 영상도네 제외.
- **모집 마감 카운트다운**: [⏱ N초 후 마감](기본 30초, 설정에서 변경)은 **메뉴 접수를 닫는 버튼일 뿐 스핀과 무관하다**. 카운트다운 동안 도네는 정상 반영되고, 0이 되면 `closed`(모집 마감) 상태가 되어 이후 도네는 반영되지 않는다. **자동으로 돌지 않으며 스트리머가 [돌리기!]를 눌러야 시작**한다. 카운트다운 중 버튼을 다시 누르면 연장, 마감된 뒤에 누르면 그만큼 다시 받는다(둘 다 횟수 제한 없음).
- [돌리기!] 버튼은 이번 판의 결과가 나온 뒤(당첨 발표·리롤 접수)에만 [🔁 다시 돌리기]로 바뀐다. [돌리기] 후 **1초 동안은 [정지!]를 누를 수 없다**(거의 돌지 않고 끝나 보이는 것 방지).
- **공정성**: 당첨은 [정지!]를 누르는 순간 `Math.random()`과 칸 수만으로 뽑는다 — 룰렛의 각도·누른 시각은 추첨에 전혀 관여하지 않으므로 **타이밍으로 결과를 고를 수 없다**(스트리머도 불가). 검증: 같은 2.000초 타이밍 3회 → 결과 제각각, 10,000회 추첨 분포가 칸 비율과 일치(50/30/15/5 → 50.4/30.1/14.5/5.1). 당첨 확률은 도장과 기록에 표시된다.
- 스핀: [돌리기] → 계속 회전 → [🛑 정지!] 누르는 순간 가중치 추첨 확정 → **10~13초** 감속(두구두구 드럼롤) → 당첨 도장 연출. [✅ 확정] 시 전용 피날레 팡파레 + 기록 탭 자동 전환.
- **도네 라우팅**: 메뉴 추가는 **모집 중(collect)에만**. 스핀 중·당첨 발표 후(접수 전)는 전부 무시, 리롤 접수 중·마감 후 대기 중에는 기준 금액 이상만 리롤권으로 인정하고 미만은 무시. 무시된 도네는 피드에 사유와 함께 기록.
- 당첨 후(decision): [🔔 리롤 도네 받기(금액 입력)] / [🔁 다시 돌리기(공짜, 기록 안 남음)] / [✅ 결과 확정] 중 스트리머가 선택. 자동 진행 없음.
- 리롤 접수: 버튼 클릭 시 **이번 회차 금액 입력**(기본 직전 금액, 확정 시 설정값 2만원 복귀 — 2만→4만→10만 에스컬레이션용). 제한시간 기본 60초.
- 리롤권: 접수 중 **단일 도네 ≥ 회차 금액** 1건당 1개 누적(합산 불인정). 마감돼도 자동 확정 안 되고, **확정 전 지각 도착분도 인정**(연동 지연 대비). 마지막 리롤이 최종. 확정 시 잔여 리롤권 소멸. 당첨 메뉴는 룰렛에 유지.
- 기록: [✅ 확정] 시에만 라운드 저장(최근 20개, localStorage), 기록 탭 + JSON 내보내기/불러오기.
- 운영: 도네 반영 ⏸ 일시정지 토글(정지 중엔 **리롤권 적립도 차단**), 전체 비우기는 진행 중 탭의 메뉴 내보내기 아래(설정 탭에서 이동), 칸 조절 클릭±1/Ctrl±10/Alt±100, 총 칸수 비노출(매출 노출 방지), 전체 비우기는 설정 탭 위험구역, 메뉴 JSON 내보내기/불러오기(예시 50종: `public/sample-menus.json`).
- 효과음: 회전 중 래칫 딸깍이(감속할수록 간격이 벌어지고 음정 상승)·드럼롤·당첨 팡파레·확정 피날레·리롤 차임·카운트다운 째깍(마지막 10초 0.5초 간격 긴박 모드)·마감 공소리. 설정 탭에서 on/off. 회전 중 BGM은 제거됨(24번 항목).
- PC(1920×1080) 전용. 새로고침/재부팅해도 진행 중 룰렛·설정·기록·피드·**라운드 상태(당첨·리롤권·접수 타이머)** 유지(localStorage).
- 단축키: Space 돌리기·정지·다시 돌리기 / R 리롤 / Enter 확정 (입력칸에 포커스가 있을 땐 무시). [확정] 시 기록 탭 자동 전환.
- **판 이름**: 룰렛 오른쪽 위를 눌러 이번 판 이름을 적는다(메인 메뉴 / 사이드 / 디저트 등). 기록에도 함께 남는다.
- **메뉴 개수 제한 없음**: 300종까지 60fps로 확인됨. 원판 글자는 100종 부근부터 겹쳐 읽기 어려워지지만, 포인터 위 이름·당첨 도장·후보 목록으로 정보는 전달된다.
- 새 버전이 배포되면 열어둔 화면도 스스로 따라간다(모집 중일 때 자동 새로고침).
- 방송 화면 연출: 당첨 도장에 **"🙌 쏜 사람: OOO · XXX"**(수동 추가분은 제외)와 **당첨 확률**, 당첨 순간 **폭죽 3연발 + 화면 전체 색종이**, 감속 절반 지점부터 포인터 아래 칸 **번쩍임**(끝으로 갈수록 강해짐). 도네로 메뉴가 붙으면 룰렛 위에 **토스트**("🍜 OOO님 → 차돌짬뽕 ×3", 최대 3개), 리롤권이 쌓이면 **구매자 이름을 금색 카드로 크게** 표시. 연결이 끊기면 화면 상단에 **빨간 경고 띠 + 경보음**(20초 throttle)과 [다시 연결] 버튼.

## 3. 아키텍처 요약

```
[GitHub Pages 정적 SPA] ──REST──> [Cloudflare Worker 프록시] ──> openapi.chzzk.naver.com
        └──────────웹소켓(Socket.IO, 도네 실시간)──직접──> 치지직 세션 서버
```

- openapi.chzzk.naver.com은 **브라우저 CORS 차단**(실측) → REST(토큰/세션/구독)만 워커 경유. 웹소켓은 직접.
- Client Secret은 **워커 Secret 환경변수**(`CHZZK_CLIENT_ID`, `CHZZK_CLIENT_SECRET`)에만 존재. 프론트/저장소에 없음.
- 프론트 프리셋: [src/config.ts](src/config.ts) (Client ID, 워커 URL). 스트리머는 아무 키도 입력 안 함 — [로그인]→공식 치지직에서 동의→[연결] 클릭 3번.
- 소스 구조: `src/state.ts`(상태 저장소+규칙 엔진+영속화 — 핵심), `roulette.ts`(Canvas 휠+스핀), `chzzk.ts`(OAuth/세션/구독/재연결), `sound.ts`(WebAudio 합성), `main.ts`(UI 와이어링), `config.ts`(프리셋), `proxy/worker.js`(+`wrangler.toml`).

## 4. 체인지로그 (2026-08-29, 커밋 순)

1. `b756527` docs: 설계 문서 (치지직 API 조사, 연동 3안 비교, 디자인 시스템)
2. `31149f6` feat: 룰렛 코어 + 규칙 엔진 + 리롤 + 기록/일시정지 + 철면수심 디자인(실제 로고/배경 에셋)
3. `0bb9417` feat: 치지직 연동 모듈 + CORS 프록시 + 리롤권 누적제 + Ctrl/Alt 칸 조절 + 파스텔 칸 색
4. `a47b377` ci: Pages 자동 배포 + README / `16d65f5` feat: 정지 버튼식 스핀 + 총 칸수 비노출
5. `aa55814` feat: 스트리머 무입력 연동(Secret→워커, config.ts 프리셋) / `d192f65`·`b093200` chore: Client ID·워커 URL 등록
6. `d7f923e` feat: 리롤 접수 수동 시작 + 설정 탭 노출(개발자 항목 UI 삭제) + 효과음 강화 + 푸터
7. `1cc9358` feat: 메뉴 JSON 입출력 + 예시 50종
8. `dc95dae` feat: 당첨 후 자유 [다시 돌리기]
9. `906a0b1` feat: 회차별 리롤 금액 입력 + 마감 후 지각 도네 인정
10. `7d779c2` feat: 카운트다운 째깍 사운드(마지막 10초 긴박 모드 + 공소리)

버그 픽스 이력: `[hidden]` CSS 무시 버그, 백그라운드 탭 rAF 정지(워치독 추가), **워커 Secret 개행 버그**(PowerShell 파이프가 `\n`을 붙여 INVALID_CLIENT 발생 → bash `printf '%s'`로 재등록해 해결).

### 2026-08-30 추가분

11. `06cae82` 룰렛 UI를 확장앱과 실측 비교 후 재구현(원색 12색 평면 원판, 허브 제거, 외부 상단 포인터 + **포인터 위 현재 항목 실시간 표시**, 라벨 이름만) + **리롤 타이머가 끝까지 흐르며 리롤권 누적**(armed 단계 제거) + **일시정지 중 리롤권 적립도 차단** + 스핀 틱 사운드 강화
12. `bc96c65`·이후 docs: `docs/공지글-모음.md` (게시판용 공지 상자, KMD26 형식)
13. `ced116f` 후보 목록에 **점유율 % (소수점 2자리)** 표시 + 헤더를 [치지직 로그인]/[치지직 로그아웃] 버튼으로 교체(설정 탭 연동 카드 삭제, 로그인 버튼은 토큰 있으면 재연결) + 효과음 마스터 게인 1.6배
14. `b9ff9d2` 리롤 대기 중 **룰렛 중앙 대형 카운트다운(10ms 단위)** — 마지막 10초 빨간 반전 + 요동 연출
15. `08541b3` **요구하지 않은 임의 제약 전수 제거**(칸 수 max99·제한시간 상한·이름 20자 컷·후보 2개 조건 등) + 최소 인정 금액 0원 설정 버그 수정. ※ 사용자 원칙: 임의 제약 금지, 필요하면 먼저 물어볼 것
16. `f4be04a` **도네이션 라우팅 확정(사용자 결정)**: 메뉴 추가는 모집 중에만. 스핀 중·당첨 발표 후(접수 전) 도네는 전부 무시, 접수 중·마감 후 대기 중에는 기준 금액 이상만 리롤권·미만은 무시. 스핀 중 대기열 제거. 무시된 도네는 피드에 사유 기록
17. `caf7d46` **최종 확정 전용 피날레 팡파레**(축포+코드 진행+아르페지오) + 정지 후 감속 **8~11초**로 연장
18. `7e378a7` 헤더 로그인/로그아웃 버튼을 **로그인 상태에 따라 하나만 표시**

### 2026-09-15 전체 검토 후 수정

19. 버그 수정: (a) 라운드 진행 중 [전체 비우기]가 타이머·리롤권·단계를 남겨두던 것 → 모집 상태로 완전 초기화 (b) 리프레시 토큰 만료 시 무한 재연결 + 재로그인 불가 → 토큰 삭제·재연결 중단·"다시 로그인" 안내, [치지직 로그인] 버튼 복귀 (c) 후원 구독 실패 시 방치 → 재연결 (d) 도네 중복 제거 창 3초 버킷 → 1초 슬라이딩(연타 유실 방지, 중복 무시도 피드 기록) (e) 칸당 금액 입력 `min=500` 잔재 제거, 오래된 안내 문구·주석 정정
20. 기능: **새로고침해도 라운드 상태 유지**(당첨·리롤권·회차 금액·접수 타이머 이어받기, `mr:round`), **피드 유지**(`mr:feed`), **단축키** Space(돌리기·정지·다시 돌리기)/R(리롤)/Enter(확정), 웹폰트 로드 후 룰렛 재그리기, 오디오 컴프레서(클리핑 방지), 접수 진행바 스케일 고정, connect() 동시 호출 가드, localStorage 항목별 손상 방어, `__store` 디버그 노출은 dev 전용, 워커 `ALLOWED_ORIGINS` 잠금(Pages+localhost)
- 사용자 결정: 소액 도네(칸당 금액 미만)가 피드 없이 버려지는 건 그대로 둠. 조작은 크롬 창에서 하므로 prompt/confirm 팝업 유지.

### 2026-09-15~18 사운드

21. 효과음 마스터 게인 2.4 + 컴프레서 뒤 메이크업 1.25. 회전음은 바람/모터 레이어(청소기 소리라 거부됨)를 거쳐 **실제 돌림판 래칫 딸깍이**로 정착: 타격 노이즈+날개 울림+저음 바디, 핀마다 ±5% 흔들림
22. 딸깍 트리거: 메뉴 ≤24개면 가상 핀 24개, >24개면 칸 경계마다(빽빽할수록 촘촘, 라벨 전환과 일치) — 사용자 제안 채택
23. 회전 속도 2.2→2.7 rev/s, 정지 후 감속 8~11초→**10~13초**
24. 회전 중 배경음(BGM)을 시도했다가 **제거함** — 참고곡(Under Siege OST "The Takeover", 진품명품 감정가 BGM) 무드의 자작 합성 루프를 넣었으나 사용자가 "정신없다"고 해 완전 삭제(`80672b3` 추가 → 다음 커밋에서 제거). 회전 중 소리는 딸깍이만 유지. 다시 요청되면 git 이력의 `sound.ts` startBgm 참고

### 2026-09-18 UI 손질

25. 한글 줄바꿈: `body`에 `word-break: keep-all`(+`overflow-wrap: break-word`) — 안내 문구가 "선/택하세요"처럼 단어 중간에서 끊기던 것 해결. `.hint`의 `word-break: break-all`(옛 URL용) 제거, 컨트롤 버튼 `white-space: nowrap` + `.controls` 줄바꿈 허용, 상태바 760px·line-height 1.45, 당첨 안내에서 리롤권 문구를 별도 줄로 분리
26. 당첨 도장: 긴 메뉴명이면 도장이 룰렛 밖으로 삐져나오던 것 수정 — `max-width:88%` + **`min-width:0`**(그리드/플렉스 기본 `min-width:auto`가 max-width를 무시함) 로 여러 줄 접힘
27. [✅ 결과 확정] 클릭 시 **기록 탭으로 자동 전환** (방금 저장된 라운드를 바로 확인)
28. 방송 연출·안전장치 3종(사용자 채택): (a) **연결 끊김 경고 띠** — 헤더 배지만으로는 방송 중 놓치므로 상단에 빨간 깜빡이 띠 + 경보음(`sound.connectionLost`, 20초 throttle) + [다시 연결] 버튼, 복구 시 짧은 차임. 재연결 백오프 최대 **60초 → 15초**로 단축, 토큰이 없을 때 무한 재시도하던 경로도 차단 (b) **리롤권 구매자 대형 표시** — 룰렛 위 금색 카드에 최근 3명 닉네임, 내용이 바뀔 때만 팝 애니메이션 (c) **도네 토스트** — 메뉴 반영 순간 룰렛 위에 크게, 3.5초 후 사라지고 최대 3개 유지. `store.emit('donation')`이 이제 `AppliedDonation{nick,amount,name,slots}`를 실어 보냄

29. 손질: (a) 리롤권 구매자 카드에 **전원 닉네임을 줄이지 않고** 표시(기존 3명+"외"), 같은 이름이 여러 번이면 `이름 ×2` (b) **익명 후원 대응** — 닉네임이 비어 오면 `익명의 후원자`로 표시하고, 중복 제거 키에 `donatorChannelId`를 써서 서로 다른 익명 후원자가 한 사람으로 묶이지 않게 함 (c) 기록 탭 행이 **펼쳐진다는 것을 드러냄** — ▶/▼ 화살표 + 호버·펼침 강조 + 상단 안내 문구

> ⚠ **익명 후원은 치지직 문서에 전혀 기술돼 있지 않다** (2026-09-18 확인: DONATION 페이로드에 `donatorNickname`/`donatorChannelId`만 있고 익명 플래그·null 여부 설명 없음). 실제로 무엇이 오는지는 방송에서 도네 피드를 보고 확인해야 한다. 코드는 빈 값·예상 밖 문자열 모두 견디도록 방어적으로 처리돼 있다.

### 2026-09-18 방송 재미·매출 강화

30. **모집 마감 카운트다운(A)** — `phase: 'closing'`(카운트다운) + `'closed'`(마감) 추가. [⏱ N초 후 마감] → 대형 카운트다운(리롤 타이머 UI·째깍 소리 재사용, 금색으로 구분) → 0이면 `closed`로 전환만 하고 **자동 스핀은 하지 않는다**(사용자 지시로 autospin 제거). 마감 버튼은 카운트다운 중엔 연장, 마감 뒤엔 재개. 타이머 필드는 리롤 접수와 공용으로 일반화(`countdownDeadline/RemainMs/DurationMs`, `startCountdown`). **검증: 마감 전 도네 반영 O, 마감 후 도네 반영 X(피드 기록), 연장·재개·수동 스핀 모두 확인**
31. **당첨 추천자 표시(C)** — 당첨 도장 아래 "🙌 쏜 사람: OOO · XXX". `'수동'`(스트리머 직접 추가)은 제외. 확정 후에도 유지되도록 `confirmedDonors` 저장·복원
32. **당첨 연출(E)** — 당첨 순간 **폭죽 3연발 + 색종이 220조각**(`celebrate()`): 룰렛 중심·좌상·우상에서 0/170/340ms 간격으로 터지며 각 46조각이 사방으로 날아간 뒤 아래로 떨어지고, 터질 때마다 섬광과 "펑" 소리(`sound.pop`). 색종이는 화면 전체(`position:fixed`)에 2.2~3.8초간 쏟아진다. 감속 **진행도 50%부터** 포인터 아래 칸이 흰색으로 번쩍이며 끝으로 갈수록 강해진다. ※ 처음엔 룰렛 영역 안 70조각·75%부터였으나 "눈에 띄지 않는다"는 피드백으로 두 차례 확대
33. 전체 비우기 버튼을 설정 탭 위험 구역 → **진행 중 탭의 메뉴 내보내기 아래 줄**로 이동(설정 탭 위험 구역 카드 삭제). 확인 창은 유지
- 보류: B(실시간 순위판·역전 연출), D(아깝게 비껴간 메뉴) — 사용자가 이번엔 제외

34. 공정성·신뢰 보강: (a) [돌리기] 후 **1초간 [정지!] 잠금**(`MIN_SPIN_MS`, `wheel.canStop`) (b) **당첨 확률 표시** — 도장에 "당첨 확률 2.65%", 기록 행에도 "🏆 메뉴 2.7%". 추첨 시점 값을 `winnerChance`로 스냅샷해 확정 후·새로고침 후에도 유지(`Round.chance`에도 저장) (c) `public/sample-donations.json` 추가 — 시청자 도네로 만들어진 상황을 재현한 예시(38종/226칸, 메뉴마다 추천자 닉네임 포함). 연습·리허설용

### 2026-09-20 성능·소리·판 이름

35. **메뉴 100종 이상에서 버벅이던 것 해결** — 매 프레임 칸·라벨을 다시 그리느라 110종에서 **7fps**까지 떨어졌다.
    회전과 무관한 원판 그림(칸·라벨·테두리)을 오프스크린 캔버스에 미리 그려 두고, 프레임마다 회전시켜 한 장만 붙인다(`ensurePlate`).
    칸 각도도 메뉴가 바뀔 때만 계산하도록 캐시(`syncSegs`, 배열 identity + 개수 + 총 칸으로 무효화).
    **측정(헤드리스 크롬): 20종·110종·300종 모두 60fps** (이전 110종 7fps).
36. 회전 속도 2.7 → **3.6 rev/s**, 감속 10~13초 → **7~9초** (시원하게 멈추도록, 사용자 요청)
37. **딸깍 소리가 프레임에 묶여 끊기던 것 해결** — 프레임마다 칸 경계를 세어 울리던 방식이라 fps가 떨어지면 소리도 같이 끊겼다.
    이제 오디오 시계에 0.25초 앞까지 미리 예약하는 스케줄러(`startTicking`/`stopTicking`)가 돌고, 빈도는 룰렛의 각속도(`wheel.tickRate`)에서 계산한다.
    초당 최대 32회로 제한(그 이상은 귀에 뭉개짐), 촘촘할 땐 한 방씩 볼륨을 낮춰 겹침을 막는다. `onPegCross` 콜백은 제거.
38. 정지 후 감속하는 동안 딸깍이 **낮은음 → 높은음으로 상승**(640Hz → 2700Hz, 지수). 타격 노이즈도 같이 밝아지고 저음 바디는 옅어진다.
39. **판 이름** — 룰렛 오른쪽 위에서 바로 입력(예: 메인 메뉴 / 사이드 / 디저트). `store.title`에 담겨 새로고침에도 유지되고,
    확정 시 `Round.title`로 기록에 남아 기록 탭 행에 태그로 표시된다. 다음 판에도 그대로 이어진다(바꾸기 전까지).
40. **이미 열어둔 화면도 새 버전 자동 반영**(`src/update.ts`) — 90초마다 `index.html`을 받아 빌드마다 바뀌는 스크립트 파일명이 달라졌는지 확인한다.
    새 버전이 뜨면 상단에 초록 띠를 띄우고, **모집 중일 때만** 3초 뒤 자동 새로고침(판이 돌아가는 중이면 끝난 뒤에 적용). [지금 새로고침] 버튼도 있다.

## 5. 다음 할 일 (우선순위순)

1. ~~실도네 E2E~~ ✅ **완료 (2026-09-18)** — 철면수심 님 계정에서 실제 도네이션 반영 정상 동작 확인
2. **익명 후원 실동작 확인** — 방송에서 익명 도네가 들어오면 피드에 어떤 이름으로 찍히는지 확인(치지직 문서 미기술). 이상하면 `chzzk.ts`의 `nick` 폴백만 고치면 됨
3. (권장) Secret이 대화에 노출됐었음 → 개발자센터에서 **재발급** 후 `cd proxy && printf '%s' '<새Secret>' | npx wrangler secret put CHZZK_CLIENT_SECRET` (개행 금지 주의!)
4. (선택) `proxy/worker.js`의 `ALLOWED_ORIGINS`에 `https://goormigrm.github.io` 넣어 프록시 잠그기 → `npx wrangler deploy`
5. (미검증 리스크) 치지직 세션 웹소켓의 Origin 정책 — 실도네 테스트에서 함께 검증됨. 문제 시 chzzk.ts의 소켓 연결 부분 확인.

## 6. 개발 환경 메모 (이 PC 기준)

- Node 24.19.0을 winget으로 설치함. 기존 셸에는 PATH 미반영 → `$env:Path = "C:\Program Files\nodejs;$env:Path"` 선행 필요.
- 빌드/검사: `npm run build` (tsc --noEmit + vite build). 로컬 실행: `npm run dev` → http://localhost:5173/mukbang-roulette/
- wrangler는 이미 이 PC에서 Cloudflare 계정(1117tkdrms) 로그인됨. 워커 재배포: `cd proxy; npx wrangler deploy`
- git 커밋 메시지에 한글+특수문자 쓸 때 PowerShell here-string이 깨질 수 있음 → 메시지 파일 + `git commit -F` 사용.
- 브라우저 자동 테스트 팁: 창이 가려지면 `document.visibilityState === 'hidden'`이 되어 **CSS 애니메이션이 아예 진행되지 않는다**(컨페티·폭죽이 안 움직여 보이는 건 버그가 아닐 수 있음 — `animation.currentTime`이 0에서 멈춰 있는지 확인할 것). `window.__store`로 스토어 직접 접근 가능(디버그용). 파일을 수정하면 vite가 리로드해 메모리 상태(피드 등)가 초기화되므로 테스트는 수정 없이 한 번에. 개발용 브라우저 패널은 workers.dev fetch를 차단하니 프록시 테스트는 curl로.
