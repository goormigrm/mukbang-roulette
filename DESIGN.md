# 철면수심 먹방 룰렛 — 설계 문서

> 치지직 스트리머 **철면수심** 전용, 도네이션 연동 먹방 메뉴 룰렛 웹앱
> 작성일: 2026-08-29 · 버전 0.1 (초안)

---

## 1. 프로젝트 개요

시청자가 치지직 도네이션으로 먹방 메뉴를 추천하면, **1,000원당 1칸**씩 룰렛에 메뉴가 추가되고, 스트리머가 룰렛을 돌려 오늘의 먹방 메뉴를 선정하는 웹 프로그램.

- 참고 UX: 크롬 확장 "돌려돌려 돌림판" (룰렛 + 항목별 확률/가중치)
- 참고 방송 포맷: 도네 금액만큼 추천 메뉴 누적 → 룰렛 스핀 → 당첨 → 제한시간 내 리롤 비용 도네 시 리롤 허용
- 배포: **GitHub Pages** (URL만 치면 바로 접속되는 정적 사이트)
- 디자인: 철면수심 차돌짬뽕 쇼핑몰(중화요리 브랜드 무드)을 따름

### 핵심 요구사항 요약

| # | 요구사항 |
|---|---|
| R1 | 룰렛(돌림판) — 항목 추가/삭제, 가중치(칸 수) 지원, 스핀 애니메이션, 당첨 연출 |
| R2 | 치지직 도네이션 실시간 수신 — 금액·닉네임·메시지 파악 |
| R3 | 1,000원당 1칸 규칙 — 예: 3,000원 + "차돌짬뽕" → 차돌짬뽕이 3칸(가중치 3) 추가 |
| R4 | 리롤 시스템 — 리롤 비용 X원·제한시간 T초 설정, 당첨 후 T초 내 X원 이상 도네 시 리롤 가능 |
| R5 | GitHub Pages에서 동작 (서버 없이, 또는 최소한의 무료 서버리스만) |
| R6 | 철면수심 전용 디자인 (중화풍 레드/블랙/크림) |

---

## 2. 치지직 API 조사 결과 (2026-08-29 확인)

공식 문서( https://chzzk.gitbook.io/chzzk )를 확인한 결과, **공식 Open API로 도네이션 실시간 수신이 가능**하다.

### 2.1 세션(Session) API — 실시간 이벤트

문서: https://chzzk.gitbook.io/chzzk/chzzk-api/session

- **연결 방식**: Socket.IO (클라이언트 1.x~2.0.3 버전 지원, `transports: ['websocket']`, `reconnection: false`)
- **연결 흐름**:
  1. `GET /open/v1/sessions/auth` (유저 인증) 또는 `GET /open/v1/sessions/auth/client` (클라이언트 인증) → 세션 접속 URL 획득
  2. Socket.IO로 연결 → `sessionKey` 수신
  3. `POST /open/v1/sessions/events/subscribe/donation?sessionKey=...` 로 후원 이벤트 구독
- **후원 이벤트 페이로드**:
  - `payAmount` — **후원 금액(원)** ← 핵심
  - `donatorNickname` — 후원자 닉네임
  - `donationText` — 후원 메시지 (→ 여기서 메뉴명 추출)
  - `donationType` — CHAT / VIDEO
  - `channelId`, `donatorChannelId`, `emojis`
- **제약**: 유저 인증 세션은 계정당 최대 3개 연결, 세션당 최대 30개 이벤트 구독

### 2.2 인증 요건 (중요)

- **후원 이벤트 구독에는 "사용자(스트리머) 계정 Access Token"이 필요** (채팅 조회·후원 조회·구독 조회는 사용자 인증 필수)
- Access Token은 치지직 개발자센터( https://developers.chzzk.naver.com )에서 애플리케이션 등록 후 발급받는 **Client ID / Client Secret**으로 OAuth 인가 코드 흐름을 거쳐 획득
- 즉, 흐름: 개발자센터 앱 등록 → 스트리머 계정으로 OAuth 로그인 → Access Token → 세션 생성 → donation 구독

### 2.3 정적 호스팅(GitHub Pages)과의 충돌 지점

OAuth 토큰 교환에는 Client Secret이 필요한데, 순수 정적 사이트에서는 Secret이 소스에 노출된다. 대응 방안 3가지:

| 방안 | 내용 | 장점 | 단점 |
|---|---|---|---|
| **A. Secret 로컬 입력** (권장 1차) | 설정 화면에서 스트리머가 본인의 Client ID/Secret을 직접 입력 → `localStorage`에만 저장, 토큰 교환을 브라우저에서 직접 수행 | 서버 완전 불필요, 개인 전용 도구라 노출 대상이 본인뿐 | 치지직 API가 브라우저 CORS를 허용하지 않으면 불가(§9 리스크) |
| **B. 서버리스 프록시** (권장 2차) | Cloudflare Workers/Vercel 무료 함수 하나가 토큰 교환·세션 발급만 대행, 프론트는 GitHub Pages 유지 | Secret 안전, CORS 문제 해결 | 배포물이 2개로 늘어남 |
| C. 비공식 채팅 WebSocket | 로그인 없이 channelId만으로 치지직 채팅 서버에 직접 접속(서드파티 오버레이들이 쓰는 방식), 도네 메시지도 채팅 이벤트로 수신 | 인증 절차 전무, 가장 간단 | 비공식 — 네이버가 언제든 막거나 구조 변경 가능 |

**결정**: A를 먼저 구현하고, CORS가 막혀 있으면 B로 전환한다. C는 A/B가 모두 막혔을 때의 비상 수단으로만 남겨둔다.

> **2026-08-29 실측 결과: 방안 B 확정.** 브라우저에서 `openapi.chzzk.naver.com` 호출을 실제 테스트한 결과 CORS가 차단되어 방안 A는 불가능. 저장소의 `proxy/worker.js`(Cloudflare Workers, 무료)를 배포하고 설정 화면의 "프록시 URL"에 입력하는 방식으로 구현했다. REST(토큰 교환·세션 발급·구독)만 프록시를 거치고, Socket.IO 웹소켓은 치지직 세션 서버에 직접 연결한다.

---

## 3. 시스템 아키텍처

```
┌─────────────────────────────────────────────┐
│  GitHub Pages (정적 SPA)                     │
│                                             │
│  ┌───────────┐  ┌────────────┐  ┌────────┐  │
│  │ 룰렛 화면  │  │ 도네 피드   │  │ 설정    │  │
│  │ (Canvas)  │  │ (수신 로그) │  │ 패널    │  │
│  └─────┬─────┘  └─────┬──────┘  └───┬────┘  │
│        │              │             │       │
│  ┌─────┴──────────────┴─────────────┴────┐  │
│  │  상태 스토어 (메뉴 목록·리롤 상태·설정)   │  │
│  └─────┬────────────────────────────────┘   │
│        │                                    │
│  ┌─────┴────────────┐   ┌───────────────┐   │
│  │ Chzzk 연동 모듈    │   │ localStorage  │   │
│  │ (OAuth·Socket.IO) │   │ (설정·백업)    │   │
│  └─────┬────────────┘   └───────────────┘   │
└────────┼────────────────────────────────────┘
         │ WSS (Socket.IO) + REST
         ▼
   치지직 Open API (openapi.chzzk.naver.com)
   - GET  /open/v1/sessions/auth
   - POST /open/v1/sessions/events/subscribe/donation
   - 후원 이벤트 push: { payAmount, donatorNickname, donationText, ... }
```

- 서버·DB 없음. 모든 상태는 브라우저 메모리 + `localStorage`(새로고침 대비 자동 백업).
- 방안 B로 전환 시 OAuth 토큰 교환 부분만 서버리스 함수로 이동, 나머지 구조 동일.

---

## 4. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 빌드 | **Vite + TypeScript** | 정적 배포에 최적, 가볍고 빠름 |
| UI | **Vanilla TS + Canvas 2D** (프레임워크 없음) | 룰렛은 Canvas가 본체라 React 등이 불필요, 번들 최소화 |
| 실시간 | `socket.io-client` **v2.x** | 치지직 세션 API가 Socket.IO 1.x~2.0.3만 지원 |
| 상태 저장 | `localStorage` | 설정·진행 중 룰렛 백업 |
| 배포 | GitHub Actions → GitHub Pages | push 시 자동 배포 |
| 사운드 | Web Audio (틱·팡파레 효과음) | 방송 연출용 |

---

## 5. 기능 상세 설계

### 5.1 룰렛 (R1)

- Canvas 2D로 원판 렌더링. **칸 단위**로 그림 — 같은 메뉴가 가중치 n이면 n칸이 연속 배치되고 같은 색으로 병합 표시(칸 수 라벨 `차돌짬뽕 ×3`).
- 스핀: `easeOutCubic` 감속, 4~7초 랜덤. 결과는 스핀 시작 시점에 가중치 비례 추첨으로 미리 확정하고 애니메이션이 그 각도에 멈추도록 역산 (프레임 드랍이 결과에 영향을 주지 않게).
- 당첨 연출: 당첨 칸 하이라이트 + 폭죽/도장 쾅 애니메이션 + 팡파레, 당첨 메뉴·추천자 닉네임 대형 표시.
- 수동 조작: 메뉴 직접 추가/삭제, 가중치 수정(클릭 ±1, **Ctrl+클릭 ±10, Alt+클릭 ±100**), 스핀 버튼. (도네 없이 테스트 가능해야 함)
- 전체 비우기 같은 파괴적 버튼은 실수로 누르지 못하게 **설정 모달의 "위험 구역"** 안에만 배치.
- 칸 색상: 메뉴가 100개 수준이어도 인접 칸이 구분되도록 황금각(137.5°) 색상환 순회, **음식에 어울리는 파스텔(마카롱) 톤** + 진갈색 라벨.

### 5.2 도네이션 → 메뉴 추가 (R2, R3)

```
후원 이벤트 수신
  → 종류 검증: donationType이 VIDEO(영상 도네)면 제외 (피드에 사유 기록)
  → 금액 검증: payAmount ≥ 최소 인정 금액 1,000원 (미만이면 제외, 상한 없음)
  → 칸 수 = floor(payAmount / 1,000)        # 1,000원당 1칸
  → 메뉴명 = donationText 정제(공백 트림, 최대 20자, 빈 문자열이면 "닉네임의 추천")
  → 동일 메뉴명 존재 시 가중치 합산, 없으면 새 항목 추가
  → 도네 피드에 기록: [닉네임] 3,000원 → "차돌짬뽕" ×3
```

- 스핀 중에 들어온 도네는 **대기열**에 쌓고 스핀 종료 후 반영.
- 리롤 대기 상태(§5.3)에서 들어온 도네: **단일 도네 금액이 리롤 비용 이상이면 리롤권 획득**(해당 도네는 리롤 결제로 소진, 메뉴 추가 안 함), 미만이면 일반 메뉴 추가로 처리. 누적 합산은 하지 않음.
- 음식 메뉴가 아닌 일반 도네이션이 섞여 들어오는 경우를 위해, 후보 목록에서 **항목별 즉시 삭제·가중치 조절**이 한 클릭으로 가능해야 함 (§5.1 수동 조작).
- 중복 이벤트 방어: 이벤트의 (donatorChannelId + 시각 + 금액) 해시로 디듀프.

### 5.3 리롤 시스템 (R4)

상태 머신:

```
[모집중] --스핀 버튼--> [스핀중] --정지--> [당첨/리롤 대기]
                                          │  (타이머 T초 카운트다운 표시)
        ┌─────────────────────────────────┤
        │ T초 내 "단일 도네" ≥ 리롤비용 X    │ T초 경과 or 스트리머 "확정" 클릭
        ▼                                 ▼
   [리롤 가능] --리롤 버튼--> [스핀중]      [확정] → 결과 고정, 다음 라운드로
```

- **리롤 도네 접수는 자동으로 열리지 않는다** (2026-08-29 변경): 당첨 발표 후 스트리머가 [🔔 리롤 도네 받기] 버튼을 눌러야 T초 타이머가 시작된다. 접수를 열지 않고 바로 [확정]도 가능. 접수 전 들어온 고액 도네는 일반 메뉴 추가로 처리.
- **접수 시작 시 이번 회차 리롤 비용 입력** (2026-08-29 추가): 버튼을 누르면 금액 입력창이 뜨고, 회차마다 올려 받을 수 있다 (예: 1차 2만 → 2차 4만 → 3차 10만). 입력한 금액은 결과 확정 시 설정 기본값으로 복귀.
- **마감 후 지각 도네 인정** (2026-08-29 추가): 치지직 연동 지연으로 도네가 몇 초 늦게 도착할 수 있으므로, 접수 마감은 자동 확정하지 않고 당첨 대기 상태로 돌아간다. 마감 이후라도 확정 전에 도착한 기준 금액 이상 단일 도네는 리롤권으로 인정되어 리롤 버튼이 켜진다. 재접수(금액 변경)도 가능.
- 설정값(자유 조절 가능): 리롤 비용 X원(**기본 20,000**), 제한시간 T초(**기본 60초**).
- 리롤 조건: **한 번의 도네이션 금액이 X원 이상**이어야 함. 소액 여러 건의 합산은 인정하지 않음.
- **리롤권 누적** (2026-08-29 변경): 제한시간 내 X원 이상 단일 도네 1건당 리롤권 1개가 쌓임. 여러 명이 후원하면 리롤을 여러 번 누를 수 있고, **마지막 리롤의 결과가 최종**. 리롤권 보유 중에는 타이머가 멈추며, 리롤 후 잔여 리롤권이 있으면 즉시 또 리롤 가능. 결과 확정 시 잔여 리롤권은 소멸.
- 리롤 시 **직전 당첨 메뉴도 룰렛에 그대로 포함**(제거하지 않음).
- 리롤 대기 화면에 **남은 시간 프로그레스 바 + "단일 도네 X원 이상 시 리롤!"** 안내와 보유 리롤권 수를 크게 표시.

### 5.4 라운드 기록 & 백업 (2026-08-29 추가 요구)

- **라운드** = 스핀 시작부터 결과 확정(확정 버튼 또는 리롤 시간 만료)까지. 확정 시점에 스냅샷 저장: 종료 시각, 당첨 메뉴, 리롤 횟수, 전체 후보 목록(이름·칸수·추천자).
- `localStorage`에 **최근 20개 라운드** 자동 보관 → 새로고침·PC 재시작 후에도 유지. 우측 패널의 **[기록] 탭**에서 최신 라운드부터 메뉴 목록·결과 확인(최소 3개 이상 항상 확인 가능).
- 진행 중인 룰렛(후보 목록·설정)도 localStorage에 실시간 백업 → 새로고침해도 현재 판 유지.
- **JSON 내보내기**: 기록 전체(최대 20개)를 `.json` 파일로 다운로드. **JSON 불러오기**: 파일 선택 시 기록 복원(최대 20개까지 표시).

### 5.5 도네이션 일시정지 (2026-08-29 추가 요구)

- 화면에 항상 보이는 **[도네 반영 ON/OFF] 토글 버튼**. OFF 상태에서 들어온 도네이션은 메뉴에 추가하지 않고 피드에 "⏸ 일시정지 중 — 반영 안 됨"으로만 기록 (음식 메뉴가 아닌 도네이션이 이어질 때 사용).
- 일시정지는 **메뉴 추가만** 차단. 리롤 대기 중의 리롤 결제 판정은 정상 동작.

### 5.6 설정 패널

- 치지직 연동: Client ID/Secret 입력, 로그인(OAuth), 연결 상태 표시(🟢 수신 중 / 🔴 끊김), 재연결 버튼
- 룰렛 규칙: 천원당 칸 수, 최소 인정 금액, 리롤 비용, 리롤 제한시간, 리롤 시 당첨 메뉴 제거 여부
- 표시: 효과음 on/off, 도네 피드 표시 줄 수
- 모든 설정 `localStorage` 저장, "설정 초기화" 제공

### 5.7 화면 구성 (단일 페이지)

```
┌────────────────────────────────────────────────┐
│  [로고: 철면수심 먹방 룰렛]        [⚙ 설정] [🔗연결]│
├──────────────────────────┬─────────────────────┤
│                          │  오늘의 후보 (목록)     │
│        ● 룰렛 원판        │  차돌짬뽕 ×3  [삭제]   │
│      (화면의 60~70%)      │  마라탕   ×1  [삭제]   │
│                          │  ...                 │
│    [돌리기!]  [리롤]       │─────────────────────│
│                          │  도네 피드 (실시간 로그) │
│  리롤 게이지 ▓▓▓░ 7,000/10,000  ⏱ 00:43        │
└──────────────────────────┴─────────────────────┘
```

- 1920×1080 방송 송출 기준으로 레이아웃 최적화. OBS 브라우저 소스로 캡처해도 보기 좋게 배경을 진하게, 텍스트 크게.

---

## 6. 디자인 시스템 (R6 — 철면수심 차돌짬뽕 무드)

참고 사이트( https://www.철면수심.com/31 ) 실측 기반:

### 6.1 컬러 팔레트

| 토큰 | 값 | 용도 (참고 사이트 실측) |
|---|---|---|
| `--ink` | `#0E0808` | 기본 텍스트·외곽 (사이트 본문색 rgb(14,8,8)) |
| `--red` | `#FE5356` | 포인트 레드 (세일 태그·강조) |
| `--red-deep` | `#FB4637` / `#C8102E` | 룰렛 칸·버튼·프레임 레드 |
| `--cream` | `#FFF8F0` | 배경 크림 (화이트 배경 + 음식 일러스트 패턴) |
| `--gold` | `#D9A441` | 중화풍 장식 라인·테두리 포인트 |

### 6.2 무드 & 요소

- **중화요리 브랜드 컨셉**: 크림 배경 위에 은은한 음식·용 일러스트 패턴(저채도 라인 아트), 콘텐츠 카드는 중화풍 이중 테두리 프레임(참고 사이트의 상품 썸네일 프레임 스타일).
- **타이포**: 타이틀은 붓글씨 계열 무료 한글 폰트(예: 나눔붓, 마포한아름/제주돋움 중 택1 — 라이선스 확인 후 self-host), 본문은 Pretendard.
- 당첨 연출에 **빨간 낙관(도장) "쾅"** 스탬프 애니메이션 — 철면수심 로고 캐릭터 느낌의 도장 프레임.
- 룰렛 칸 색: 레드 계열 2톤(`--red-deep`/`--red`) + 블랙 + 크림 교차 배치로 브랜드 톤 유지.
- 다크 배경 아님 — 사이트와 동일하게 밝은 크림 기조, 방송 화면에서 잘 보이도록 대비 강하게.

---

## 7. 저장소 구조 & 배포

```
mukbang-roulette/
├─ index.html
├─ src/
│  ├─ main.ts              # 엔트리, 상태 스토어
│  ├─ roulette/            # Canvas 룰렛 (렌더·스핀·추첨)
│  ├─ chzzk/               # OAuth·세션 연결·이벤트 파서
│  ├─ reroll.ts            # 리롤 상태 머신
│  ├─ ui/                  # 설정 패널·도네 피드·연출
│  └─ styles/
├─ public/                 # 폰트·효과음·패턴 이미지
├─ .github/workflows/deploy.yml   # Pages 자동 배포
└─ DESIGN.md (이 문서)
```

- `main` push → GitHub Actions가 `vite build` → `gh-pages` 배포.
- 접속 URL: `https://<계정>.github.io/mukbang-roulette/` (원하면 커스텀 도메인 연결).

---

## 8. 개발 로드맵

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| M1 | 룰렛 코어 — Canvas 원판, 수동 항목 추가, 가중치, 스핀·당첨 연출 | 도네 없이 돌림판으로 완전 동작 |
| M2 | 치지직 연동 — 개발자센터 앱 등록, OAuth, 세션 연결, donation 구독 (방안 A, 막히면 B) | 실도네/테스트 도네가 피드에 찍힘 |
| M3 | 규칙 엔진 — 1,000원=1칸 반영, 스핀 중 대기열, 리롤 상태 머신·타이머·게이지 | §5.2~5.3 시나리오 전부 통과 |
| M4 | 디자인 입히기 — §6 디자인 시스템, 연출(도장·사운드) | 방송 화면 기준 시안 확정 |
| M5 | 배포·실전 테스트 — Pages 배포, 실제 방송 리허설 | 스트리머 계정으로 E2E 성공 |

---

## 9. 리스크 & 미해결 질문

| 리스크 | 영향 | 대응 |
|---|---|---|
| 치지직 Open API의 브라우저 CORS 미허용 | 방안 A 불가 | M2 초기에 즉시 검증, 막히면 방안 B(서버리스)로 전환 |
| Access Token 만료(세션 유지) | 방송 중 수신 끊김 | refresh token 자동 갱신 + 연결 상태 표시·자동 재연결 |
| Socket.IO 구버전(2.x) 의존 | 최신 라이브러리와 충돌 | v2 클라이언트 고정 설치, 격리 모듈로 캡슐화 |
| 개발자센터 앱 심사/권한 범위 | 후원 스코프 승인 지연 | M2 시작 시 가장 먼저 앱 등록 신청 |
| 도네 메시지에 메뉴명이 없는 경우 | 빈 항목 | "닉네임의 추천" 폴백 + 스트리머가 목록에서 이름 수정 가능 |

**확정된 운영 규칙 (2026-08-29 결정)**
1. 리롤 기본값: 비용 **20,000원** / 제한시간 **60초** — 설정 패널에서 자유 조절 가능
2. 리롤 조건: **단일 도네이션** 금액이 리롤 비용 이상일 때만 (누적 합산 불인정)
3. 리롤 시 직전 당첨 메뉴는 룰렛에 **유지**
4. 최소 인정 금액 **1,000원**, 상한 **없음**
5. 영상 도네(donationType=VIDEO)는 **불인정**
6. 음식 메뉴가 아닌 도네이션은 후보 목록에서 **수동 삭제/조절이 쉽게** 가능해야 함
